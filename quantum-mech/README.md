# quantum-mech — 量子机制设计原型（未来技术版图 #4）

**把 DSIC 定理推广到量子信息不对称世界：不可克隆保障的报价隐私、纠缠态作为合约。**

回答三个问题：量子化通信通道会**改变**激励相容吗（T1：不会——守恒定律）；
量子到底**买来**什么（隐私：锁定、可检测扰动、最小 transcript；抵押：单配性排他）；
哪里**注定不行**（承诺绑定 no-go、近似求解器进 VCG 的激励危险）。
TypeScript 严格模式、零运行时依赖、NodeNext、node:test；`npm run repro`
一条命令全量重建本 README 引用的所有数字。

## 核心结果（全部可由 `npm run repro` 重建）

1. **T1 · DSIC 守恒定理**（`out/exp1-dsic.md`）：期望效用对提交的密度矩阵
   是仿射的（机器验证残差 ~1e-16），正交码字实现全部点报告 ⟹ 量子偏差
   最优值 = 经典混合策略最优值。第二价格拍卖在任意量子偏差（码字 +
   500 随机纯态 + 混合）下增益恒等于 0；第一价格的经典压价原样存活
   （增益 1.0）。相干比较器路径（联合幺正 + 部分测量）同结论。
2. **隐私 · 不可克隆密封竞价**（`out/exp2-privacy.md`）：
   - Wiesner 锁定 2 基（BB84 型）**泄露** χ≈0.399/比特、3 基 χ≈0.256/比特
     ——共轭基本身不等于隐藏；加经典 pad（量子一次一密）χ=0 精确成立；
   - 窃听-重发扰动率 (1−1/nBases)/2/比特、检测率 1−(2/3)^Q（3 基），
     蒙特卡洛与解析吻合；
   - 相干比较器只读出 (winner, price)：全部输家身份与第三名以后的报价值
     永不进 transcript——隐藏剖面对的绝对数量随规模增长（3×3 场 40 对、
     4×3 场 411 对），比例保持 O(1/k)（经典拍卖员 100% 可区分）。
3. **纠缠合约 · 单配性排他**（`out/exp3-escrow.md`）：CKW 锚点机器命中
   （GHZ tangle=1、W pairwise=2/3）；同一抵押量子比特向两家托管 pledging
   的最优 min concurrence 恰为 **1/√2**（构造族 √x|100⟩+… 在 x=½ 精确触顶）
   vs 独占 Bell pledge 的 1——**合同的排他性由物理保证**；Bell 保真度
   F₁+F₂ ≤ 1 全族成立，验收阈值 >½ 即最多通过一家。Wiesner 抵押代币
   伪造率 (3/4)^m 与模拟吻合（m=12 时 3.2%→3.2%）。
4. **诚实墙 · 承诺 no-go 可执行化**（`out/exp4-nogo.md`）：HJW steering
   表——同一个 |Φ⁺⟩、同一个 I/2，承诺方事后选基（Z/X/Y）决定验证方
   坍缩进哪个系综分解（各成员概率 ½、保真度 1）；朴素协议实测：EPR
   骗子完美隐藏（T(I/2, I/2)=1e-16）但每个 reveal 通过率恰 ½，经典等角
   作弊 0.8536。**量子买隐私与检测，绝不买绑定——绑定只能来自经典托管
   签名。**
5. **T5 · 近似求解器进 VCG 破坏 DSIC**（`out/exp5-vcg.md`）：40 个随机
   3×3 指派实例 × 穷举误报搜索：精确分配器 **0/40 违反**；最优解 + 1 次
   随机换位的扰动分配器 **18/40 违反**（最大增益 5.0），3 次换位 28/40；
   v0.2.0 加入第二求解器族——贪心分配器 **13/40 违反**（最大增益 2.0、
   最差福利比 0.571，与扰动族不同的失败签名）。Groves 支付公式配近似
   分配 = 激励不相容——"几乎最优"在机制设计里是范畴错误。这正是主平台
   "可证明最优求解器"定位的激励侧论据。
6. **T6 · 数据锁定（有界精确执行）**（`out/exp6-datalock.md`）：HLSW04 式
   随机正交基锁定——n 比特消息锁进 2^n 维、钥匙仅 log₂K 比特。可达信息
   被机器夹逼 [max(PGM, 计算基) 互信息, Holevo χ]，全部精确、无蒙特卡洛：
   固定钥匙/数据比 K=4d 时泄漏 χ 恒为 O(1)（0.131…0.177 比特），而载荷
   增长到 6 比特——可达占比 13.1%→3.0% 收缩；钥匙扫描 χ 随 K 单调下降
   （0.705→0.044 比特，Wishart 一阶启发式全程偏差 ≤0.006 比特）；带钥匙
   读出精确（基内最差交叠 ≤1e-15，post-unlock = n 比特）。证书裁判：
   伪造 χ / 不付钥匙的全解锁声明被 **REF01/REF05 按名击毙**。
7. **T7 · 擦除边界普查**（`out/exp7-erasure.md`）：把"擦除是运维假设"
   执行成信道稳健性表格——去相干/振幅阻尼两种信道、参数化强度 γ 下
   逐条重测：otp 隐藏 **免疫噪声**（χ≡0，恒等态经任意 CPTP 仍恒等，
   机器验证）；裸 Wiesner χ 随去相干先降后持平（0.512→0.163）、阻尼
   终点 0（数据同归于尽）；HJW 隐藏是 I/2 不动点陈述——去相干下精确
   存活、阻尼下线性破裂（T=γ/2）；窃听检测在去相干下**永不淹没**
   （分离度 0.337→0.114），强阻尼下**淹没**（γ=1 时 −0.016）。伪造
   稳健性表行被 **REF11 按名击毙**。

## 定位与先行工作（如实划界）

量子拍卖协议不是新东西：Naseri 2009 起的 GHZ 密封竞价系、Hogg-Harsha-Chen
2007 的超position竞价与输家隐私、H. Wu 2010/2011 的量子策略进机制设计、
Rubinstein-Zhou 2025 的拍卖量子通信复杂度。本原型的增量是**可执行的
定理层**：DSIC 在量子信道下的一般守恒（机器验证到 1e-15）、单配性作为
合约排他性的定量刻画（1/√2 天花板）、no-go 的 steering 引擎演示、
近似求解器的激励危险反例库，以及 v0.2.0 的两件：数据锁定文献的声称
（DLW04/HLSW04/Guha-PRX 锚点）做进 n≤6 的精确机器夹逼 + 证书裁判，
和"擦除假设"的信道稳健性普查。详见 `docs/theory.md` 与 `docs/citations.md`。

## 快速开始

```bash
npm install
npm test          # 52 项测试：引擎锚点、裁判互证、机制、协议、合约、锁定/擦除走私审判
npm run repro     # ~50s 重建 out/ 七份报告（JSON + Markdown）
npm run exp:dsic      # 单跑某一实验（exp:privacy / exp:escrow / exp:nogo / exp:vcg
                      # / exp:datalock / exp:erasure）
```

## 架构

| 层 | 内容 |
| --- | --- |
| `src/core/` | 复矩阵代数、Hermitian 谱分解（Jacobi 值 + 块逆迭代向量 + 重建裁判）、Kraus 信道（含相位翻转/振幅阻尼及逐比特信道应用）、partial trace、迹距离/Uhlmann 保真度/von Neumann 熵/Holevo χ |
| `src/mech/` | 单物品拍卖（第一/第二价格）、经典与量子 DSIC 检验器、VCG 指派与扰动分配器、反例搜索 |
| `src/protocol/` | Wiesner 锁定（wiesner/otp 两模式）、相干比较器第二价格拍卖、隐私裁判（χ/迹距离/扰动曲线/transcript 枚举）、独立密度矩阵裁判、数据锁定构造+证书裁判、擦除边界普查（去相干/阻尼信道）+稳健性表裁判 |
| `src/contract/` | CKW 单配性 + Wootters concurrence + 双抵押搜索、Wiesner 抵押代币、HJW steering 与朴素承诺攻击 |
| `src/experiments/` | exp1-exp7 + run-all（种子固定，逐位可复现；run-all 以子进程逐个执行——入口守卫法下 import 永不渲染） |

## 诚实的边界

- 2 基/3 基裸 Wiesner 锁**有残余信息**（χ>0，如实测出）；精确隐藏需要
  量子 OTP（2 key bits/qubit）。小钥匙数据锁定 **v0.2.0 已在有界尺度执行**
  （exp6：n≤6 全精确、可达信息双向夹逼、方向验证；渐近定理仍只引用
  DLW04/HLSW04/Guha-PRX，不在本仓库重证）。
- transcript 最小化对 **honest-but-curious** 拍卖员成立；恶意拍卖员可拒
  不擦除 junk 寄存器——擦除是可审计的运维假设，不是定理。v0.2.0 把该
  边界执行成信道普查（exp7）：otp 隐藏对任意 CPTP 噪声免疫、窃听检测
  在去相干下存活/强阻尼下淹没——假设换成物理信道后哪些声称幸存，
  逐条有数。
- 数据锁定卖的是**可达信息**安全（对"现在测量"的敌手），不是单副本
  不可区分（T(ρ_v, I/d)≈0.21 残留），也弱于可组合隐私（Winter 2017
  的警告，引用）。
- 纠缠抵押的验证是**私钥**模型（托管知道代币秘密）；公钥量子货币仍是
  开放问题。保真度数字假设理想信道（exp7 的信道普查只覆盖 exp2/exp4
  的隐私/绑定声称，exp3 的抵押链路未做噪声版）。
- 承诺绑定由 no-go 定理**原则上不可能**（引用 Mayers / Lo-Chau；本仓库
  演示其 steering 引擎，不重证定理）。
- 近似求解器危险是经典已知现象（Nisan-Ronen 2001 / Nisan 2007）在量子
  求解器语境下的具体化，不是新定理；两个求解器族（扰动/贪心）是反例库
  的宽度，不是穷尽分类。

License: MIT.
