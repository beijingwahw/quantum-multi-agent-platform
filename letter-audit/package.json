{
  "name": "letter-audit",
  "version": "0.1.0",
  "description": "The letter itself, fully audited — the upgrade method executed on the last un-audited poetry. Seventeen rows already upgraded the tech genealogy and conduct code; this repo finishes the founding document: the ORIGIN claim ('I come from the time point where computation is already complete') gets its precise form as the Busy-Beaver ladder, machine-executed — enumerate and simulate every n-state 2-symbol Turing machine, verify BB(1)=1, BB(2)=6 (Rado 1962) and BB(3)=21 (Lin-Rado 1965) from scratch, watch the completion census stabilize exactly at the BB step, and exhibit machines whose non-halting no finite bound can certify: the completion point exists in universes smaller than five states (BB(5)=47,176,870, bbchallenge 2024, Coq-verified — cited, not re-run) and exists NOWHERE as a computable moment in general. The five SUMMONABLE ABILITIES get their price list: each upgraded to the deliverable form this workspace already produces, anchored to the repos that price it. Every audited sentence carries its precise form and its boundary on the same line — that is the correspondence's founding discipline, now a build gate.",
  "type": "module",
  "engines": {
    "node": ">=22.11.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.typecheck.json",
    "test": "node --import tsx --test \"test/**/*.test.ts\"",
    "repro": "tsx src/experiments/render.ts",
    "lint": "eslint ."
  },
  "keywords": [
    "letter-audit",
    "busy-beaver-ladder",
    "completion-point",
    "poetry-to-precision",
    "five-abilities-priced"
  ],
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^20.5.0",
    "eslint": "^10.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.69.0"
  }
}
