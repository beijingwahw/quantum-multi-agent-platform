# THE PRE-FLIGHT CARD — the genealogy, forward-facing

> DELIVERY PROTOCOL, STEP 0: `npm run preflight -- <repo>` before building in a repo — the rules below are the mistakes that repo already made. Coverage is law (G5): every family sighted in the last ten batches is on its repo's card; this run prints ONE repo, hot families (sighted within the last three batches) first.


## burial-record

HOT (2 families sighted within the last three batches): cat:toolchain, cat:citation-drift

| family | sightings | latest | held by | the rule (the latest correction) |
| --- | --- | --- | --- | --- |
| cat:toolchain | 64 | b92#0 (b92) | BOOKED-UNENFORCEABLE | the collateral reverted and the real slots set through the Edit tool with the context lines visible — a version edit quotes its target's own context, never a bare digit pattern across a whole file (the anchors board's own lesson: a needle must quote its target's own digits, not a dependency's) |
| cat:citation-drift | 20 | b92#1 (b92) | GATE-ENFORCED | both slots pinned to the live version with the bump and the census needle repointed to follow — the books-agree pattern the lockfile family already proved in its siblings (stable-world's T11, dtc-clock's permanent guard): the manifest and the lock's two slots are one number or the suite names the drift |
| count-drift | 3 | b90#0 (b90) | GATE-ENFORCED | npm run repro re-rendered with the checker green and the refresh committed in the same breath it was found (383c65a), and the books-agree guard now holds the class: the suite renders fresh and asserts byte-identity with the committed artifact on every run — a derived artifact is a data copy of its source, and the count-drift law owns it exactly as it owns prose counts |
| cat:process | 84 | b88#3 (b88) | BOOKED-UNENFORCEABLE | the cast deleted before boarding — self-review is the pre-machine factory check; drafts land finished or not at all |
| cat:wrong-object | 30 | b88#9 (b88) | GATE-ENFORCED | L5 holds the error face at the ledger's own gate — a malformed appeal package.json is BOOKED as a violation, never a crash; the trial fires on every suite run |
| cat:statistics | 31 | b87#25 (b87) | BOOKED-UNENFORCEABLE | the linear term strict, the coupling optional (?) — the guard's domain read from the format it serves, the fixture green again at the suite's first run |
| cat:dimension-slot | 20 | b87#28 (b87) | GATE-ENFORCED | XVAL_SHELL_RANGE names the range at both kernels' doors — the silent-NaN hole trial fires on every suite run |
| runner-path | 8 | b87#10 (b87) | BOOKED-UNENFORCEABLE | the in-repo scratch script written, run, deleted — the -e shape leaves no output and no artifact; the scratch-file rule is the route |
| cat:anchor-blindspot | 3 | b87#18 (b87) | GATE-ENFORCED | the diff asserts re AND im — the anchor now lives in the space that can actually fail |
| cat:bogus-comparison | 1 | b87#13 (b87) | GATE-ENFORCED | EA:TOWER-SHAPE refuses the non-positive height at construction and EA:TOWER-DOMAIN refuses the hand-built smuggle at the comparison — defense in depth, both firing on every suite run |
| shell-template-heredoc | 7 | b85#4 (b85) | BOOKED-UNENFORCEABLE | code and data files travel by Write/Edit only, no content-based exceptions — clean-after-the-fact does not absolve (the b79#10 judgment); the heredoc's own exit 49 was the channel saying so itself |
| edit-anchor | 5 | b85#12 (b85) | BOOKED-UNENFORCEABLE | version bumps travel by Edit on a Read file, both lock slots included — verified clean does not absolve the channel (the b79#10 judgment) |
| cat:machine-overruled | 14 | b84#1 (b84) | GATE-ENFORCED | the machine overruled the assertion's discriminant — === where signed zero is one value, with the comment saying why (fix the anchor's comparison, not the machine; the b13#7 law's face) |
| cat:conjugation | 6 | b79#3 (b79) | GATE-ENFORCED | the one-sided limits are priced exactly WITH the signed win-side limit (winner fromWin 0 / fromLose -1/5; loser fromWin -1/10 / fromLose 0), both dsicAcross — the sign convention pinned by the exact rationals on every run |
| tautological-witness | 1 | b79#16 (b79) | GATE-ENFORCED | convicted by the new tautology smuggling trial: a formula-times-itself witness certifies the WRONG constant |cos 3*delta|^k just as happily — named, rejected, the negative control fires on every run; the coherent law carries its two-road needle and the dephased law its exhaustive witness |

Reports freshness (the repro-no-op face, v0.22.0): 1 reports on disk; newest render 2026-09-11T20:59:37.934Z; src tree newest 2026-09-11T14:35:53.219Z — FRESH (the newest render postdates every source)